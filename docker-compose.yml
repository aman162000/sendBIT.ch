version: "3.8"
services:
    nodeserver:
        image: aman162000/node
        build:
            context: ./
            
        ports:
            - "3000:3000"
    nginx:
        image: aman162000/sendbit-nginx
        restart: always
        build:
            context: ./nginx
        ports:
            - "80:80"